[project]
name = "hermes-maintenance-loops"
version = "0.2.0"
description = "Portable read-only health auditing and suggestion-only improvement evaluation for Hermes maintenance workflows"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "Hermes Maintenance Loops contributors"}]
dependencies = []

[project.scripts]
hermes-maintenance-loops = "hermes_maintenance_loops.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
